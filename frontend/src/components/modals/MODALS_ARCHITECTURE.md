# Архітектура модалок

## 🎯 Основний принцип: ПРОСТОТА

Кожна модалка має **одну чітку відповідальність** і **простий інтерфейс**.

---

## 📚 Ієрархія модалок

### 1. **CreateIngredientModal** (листова модалка)
**Файл:** `frontend/src/components/modals/CreateIngredientModal.tsx`

**Призначення:** Створення нового інгредієнта (food)

**Стан:**
- Форма з полями (name, calories, protein, fat, carbohydrate)

**Дії:**
- `onCreateIngredient(food)` - повертає створений інгредієнт
- `onClose()` - закриває модалку
- Диспатчить `createFoodRequest` в Redux

**Немає дочірніх модалок.**

---

### 2. **SelectIngredientModal** (листова модалка)
**Файл:** `frontend/src/components/modals/SelectIngredientModal.tsx`

**Призначення:** Вибір інгредієнта зі списку або відкриття створення нового

**Стан:**
- Пошук по інгредієнтам

**Дії:**
- `onSelectIngredient(food, weight)` - повертає вибраний інгредієнт
- `onCreateIngredient()` - відкриває CreateIngredientModal (батько керує)
- `onClose()` - закриває модалку

**Немає дочірніх модалок.**

---

### 3. **CreateDishModal** (самодостатня модалка)
**Файл:** `frontend/src/components/modals/CreateDishModal.tsx`

**Призначення:** Створення нової страви з інгредієнтами

**Стан:**
- `dishName` - назва страви
- `ingredients[]` - масив інгредієнтів
- `showSelectIngredient` - показувати SelectIngredientModal
- `showCreateIngredient` - показувати CreateIngredientModal

**Дії:**
- `onSuccess(dish, ingredients)` - повертає створену страву (опціонально)
- `onClose()` - закриває модалку
- Диспатчить `createDishRequest` в Redux

**Дочірні модалки:**
- `SelectIngredientModal` - для вибору інгредієнтів
- `CreateIngredientModal` - для створення нових інгредієнтів

**Логіка:**
1. Користувач вводить назву страви
2. Додає інгредієнти (кожен з вагою)
3. Вага страви = сума ваг інгредієнтів (авто-розрахунок)
4. Натискає "Create"
5. Створюється dish і зберігається в Redux
6. Опціонально: викликає `onSuccess` для батька (наприклад, AddMealModal)

---

### 4. **SelectDishModal** (листова модалка)
**Файл:** `frontend/src/components/modals/SelectDishModal.tsx`

**Призначення:** Вибір страви зі списку або відкриття створення нової

**Стан:**
- Пошук по стравам

**Дії:**
- `onSelectDish(dish)` - повертає вибрану страву
- `onCreateDish()` - відкриває CreateDishModal (батько керує)
- `onClose()` - закриває модалку

**Немає дочірніх модалок.**

---

### 5. **AddMealModal** (кореневе дерево)
**Файл:** `frontend/src/components/modals/AddMealModal.tsx`

**Призначення:** Додавання прийому їжі (сніданок/обід/вечеря) зі стравами

**Стан:**
- `mealDishes[]` - масив страв у цьому прийомі їжі
- `showSelectDish` - показувати SelectDishModal
- `showCreateDish` - показувати CreateDishModal

**Дії:**
- `onAddMeal(meal)` - повертає створений прийом їжі
- `onClose()` - закриває модалку

**Дочірні модалки:**
- `SelectDishModal` - для вибору страв
- `CreateDishModal` - для створення нових страв

**Логіка:**
1. Користувач додає страви до прийому їжі
2. Кожна страва може бути:
   - Вибрана зі списку (через SelectDishModal)
   - Створена нова (через CreateDishModal)
3. Список страв відображається з інгредієнтами
4. Натискає "Add Meal"
5. Створюється meal і передається батьківському компоненту

---

## 🔄 Потоки взаємодії

### Потік 1: Створення страви на DishesPage
```
DishesPage
  → CreateDishModal (open)
    → SelectIngredientModal (add ingredient)
      → вибрати існуючий інгредієнт
      OR
      → CreateIngredientModal (create new)
        → створює і додає до страви
    → Create dish
  → Dish додано в Redux
```

### Потік 2: Додавання прийому їжі з існуючою стравою
```
MainPage
  → AddMealModal (open)
    → SelectDishModal (add dish)
      → вибрати існуючу страву
    → Add Meal
  → Meal створено
```

### Потік 3: Додавання прийому їжі з новою стравою
```
MainPage
  → AddMealModal (open)
    → SelectDishModal (add dish)
      → "Create your own dish"
        → CreateDishModal (open)
          → SelectIngredientModal (add ingredient)
            → вибрати/створити інгредієнт
          → Create dish
        → Dish додано до meal
    → Add Meal
  → Meal створено з новою стравою
```

---

## ✅ Правила архітектури

### 1. **Один стан = один boolean**
Замість складних union type `modalContext`, використовуємо прості boolean прапорці:
```typescript
// ❌ ПОГАНО
type ModalContext = 
  | { type: 'none' }
  | { type: 'select', id: number }
  | ...

// ✅ ДОБРЕ
const [showSelectIngredient, setShowSelectIngredient] = useState(false);
const [showCreateIngredient, setShowCreateIngredient] = useState(false);
```

### 2. **Модалки не знають про своїх нащадків**
Батьківська модалка **не керує внутрішнім станом** дочірніх модалок.

```typescript
// ❌ ПОГАНО - батько передає стан і логіку
<CreateDishModal
  ingredients={parentIngredients}
  onAddIngredient={() => setShowIngredient(true)}
  onUpdateWeight={(idx, w) => updateParentState(idx, w)}
/>

// ✅ ДОБРЕ - модалка автономна
<CreateDishModal
  isOpen={showCreate}
  onClose={() => setShowCreate(false)}
  onSuccess={(dish, ingredients) => handleNewDish(dish, ingredients)}
/>
```

### 3. **Callbacks повертають результат, а не керують станом**
```typescript
// ❌ ПОГАНО
onAddIngredient={() => setModalContext({type: 'select', id: 'new'})}

// ✅ ДОБРЕ
onSuccess={(dish, ingredients) => addDishToMeal(dish, ingredients)}
```

### 4. **Кожна модалка скидає свій стан при закритті**
```typescript
const handleClose = () => {
  // Reset internal state
  setDishName("");
  setIngredients([]);
  setShowSelectIngredient(false);
  
  // Notify parent
  onClose();
};
```

### 5. **Диспатч + callback для гнучкості**
Модалки створення (Create*) виконують дві дії:
1. **Диспатч у Redux** - для збереження в store
2. **Callback** - для локального використання батьком (опціонально)

```typescript
dispatch(createDishRequest(dishData));
onSuccess?.(dish, ingredients); // опціонально
```

---

## 🎨 UI принципи

### Z-index ієрархія
```
z-40  - AddMealModal (базова модалка)
z-50  - SelectDishModal, CreateDishModal (1 рівень)
z-60  - SelectIngredientModal (2 рівень)
z-70  - CreateIngredientModal (3 рівень)
```

### Transition classes
Всі інтерактивні елементи мають `transition-colors` для плавних переходів.

### Disabled states
Кнопки з `disabled` мають:
```css
disabled:bg-gray-300 disabled:cursor-not-allowed
```

---

## 📝 Коментарі в коді

Кожна модалка має JSDoc коментар з поясненням:
```typescript
/**
 * Self-contained modal for creating a new dish with ingredients.
 * Manages its own ingredient selection/creation modals internally.
 */
```

---

## 🧪 Тестування

Для тестування кожного потоку:

1. **DishesPage → Create Dish**
   - Відкрити модалку
   - Додати інгредієнти (існуючі + нові)
   - Створити страву
   - Перевірити Redux store

2. **AddMealModal → Select Existing Dish**
   - Відкрити модалку
   - Вибрати існуючу страву
   - Додати meal
   - Перевірити callback

3. **AddMealModal → Create New Dish**
   - Відкрити модалку
   - Створити нову страву з інгредієнтами
   - Додати meal
   - Перевірити callback

---

## 🔧 Технічні деталі

### TypeScript типи
```typescript
// Dish з опціональними інгредієнтами для UI
export type DishWithFoods = Dish & {
    foods?: DishFood[];
};
```

### useCallback для handlers
Всі обробники подій обгорнуті в `useCallback` для оптимізації:
```typescript
const handleCreate = useCallback(() => {
  // ...
}, [dependencies]);
```

### useMemo для обчислень
Автоматичний розрахунок ваги:
```typescript
const totalWeight = useMemo(() => {
  return ingredients.reduce((sum, ing) => sum + (ing.weight || 0), 0);
}, [ingredients]);
```

---

## 🎯 Резюме

**До:** Складна система з union types, вкладеними контекстами і заплутаною логікою переходів.

**Після:** Проста ієрархія незалежних модалок з чіткими інтерфейсами та boolean прапорцями.

**Результат:** Код легко читається, тестується і підтримується. Кожна модалка робить одну річ і робить її добре.

